export class Post{

    id:number;
    title:string;
    author:string;

    constructor(id, title, author){

        this.id = id;
        this.title = title;
        this.author = author;
    }
}